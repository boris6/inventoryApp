@page
@model InventoryApp.Pages.Allocation.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Allocation[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Allocation[0].CreatedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Allocation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AllocationID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AllocationID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AllocationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
